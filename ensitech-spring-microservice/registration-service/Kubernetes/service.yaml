apiVersion: v1

kind: Service

metadata:

  name: registration-service

spec:

  selector:

    app: registration-service

  ports:

    - protocol: TCP

      port: 8086

      targetPort: 8086

  type: ClusterIP