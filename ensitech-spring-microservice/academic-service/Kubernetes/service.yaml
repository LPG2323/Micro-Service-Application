apiVersion: v1

kind: Service

metadata:

  name: academic-service

spec:

  selector:

    app: academic-service

  ports:

    - protocol: TCP

      port: 8085

      targetPort: 8085

  type: ClusterIP