apiVersion: v1

kind: Service

metadata:

  name: training-service

spec:

  selector:

    app: training-service

  ports:

    - protocol: TCP

      port: 8084

      targetPort: 8084

  type: ClusterIP